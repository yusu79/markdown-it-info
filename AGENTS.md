# AGENTS.md

## 基本方針

* ユーザーとのやり取りは日本語で行う
* 既存のプロジェクト構成とコーディングスタイルに従う
* 実装前に関連コードと既存仕様を確認し、影響範囲を把握する
* 依頼を達成するために必要な最小限の変更にとどめる
* 依頼と関係のないファイルやコードは変更しない
* 必要がない限り、新しい依存パッケージを追加しない
* ソースコードや既存資料から確認できる事項を推測で判断しない
* ユーザーから見た動作、公開API、互換性などに影響する判断が必要な場合は、勝手に決定せず実装前に方針を提示する
* 内部実装上の軽微な判断は、既存コードや既存の設計方針に従って決定してよい
* 作業中に依頼と無関係な問題を発見した場合は、勝手に修正せず報告する
* 実装後は `package.json` に定義されているテストと lint を実行し、結果を確認する。


## 参照するルール

開発手順、ブランチ運用、テスト、Lint、コミット、公開については `CONTRIBUTING.md` に従う。

Codexの作業手順については `docs/agent-workflow.md` に従う。

## リリース時の確認

`release` ブランチまたは `hotfix` ブランチでリリース作業を行う場合は、公開前に `package.json` の拡張機能識別情報とバージョンを確認する。

以下を必ず確認する。

1. `release-1.0.0` 時点の `name`

   * このプロジェクトではプロジェクト名と一致しているか確認する

2. 現在の `release-*` または `hotfix-*` と以前のリリースの `name`

   * 過去の公開済みバージョンと一致していることを確認する
   * 意図せず変更されている場合は公開を行わず、ユーザーへ報告する
   * `name` が異なると、Marketplaceなどで既存拡張機能の更新ではなく別の拡張機能として登録される可能性があるため、特に注意する

3. `hotfix-*` の `version`

   * `hotfix` ブランチでは、修正内容に対応するリリースバージョンへ `package.json` の `version` を更新する
   * 原則として、現在公開されているバージョンからパッチバージョンを上げる
   * 例: `1.2.0` に対する修正リリースであれば `1.2.1` とする
   * すでに同じバージョンが公開済みの場合は、そのまま公開せずユーザーへ報告する
   * `hotfix` ブランチ名にバージョンが含まれている場合は、ブランチ名のバージョンと `package.json` の `version` が一致していることも確認する


### 公開作業の担当

リリース時の公開作業はユーザーが担当する。

`task-list.md` には公開作業をタスクとして残し、「ユーザー担当」と明記する。
Codexは以下を実行しない。

- `main` とリリースタグのpush
- GitHub Actionsの公開ワークフロー実行
- npmへの公開
- 公開結果の確認

ユーザーから完了報告を受けたら、公開タスクを完了へ更新し、後続タスクへ進む。

### 変更範囲

依頼された作業に必要なファイルだけを変更する。

作業中に依頼と関係のない問題を発見した場合は、
勝手に修正せずユーザーに報告する。


## プロジェクト概要

Markdown Infoは、VS CodeのMarkdownプレビューで情報・警告などのInfoボックスを表示するための拡張機能です。

主にQiita/Zenn風のコンテナ記法を扱います。

```markdown
:::note info タイトル
内容
```

````

```markdown
:::message warn タイトル
内容
:::
````

対応するタイプは次の4種類です。

* `info`: 一般的な情報
* `warn`: 警告
* `alert`: 強い警告
* `question`: 質問・補足

主な機能は以下です。

* `markdown-it-info` を使ったMarkdownプレビューへのInfoボックス描画
* Infoボックスの入れ子
* タイトル未指定時のデフォルトタイトル設定
* `default` / `qiita` / `zenn` のプレビュースタイル切り替え

VS Codeの標準Markdownプレビューを拡張する構成で、`markdown.markdownItPlugins` と `markdown.previewStyles` を利用しています。

現在のバージョンは `1.1.3` です。

`package.json` 上でもMarkdown-itプラグイン、3種類のPreview Style、Markdownスニペット、`defaultTitle` の設定が定義されています。 スニペットでは `info / warn / alert / question` を選択する構成です。
